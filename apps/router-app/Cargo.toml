[package]
edition = "2024"
name = "fncc-router-app"
publish = false
version = "0.0.0"

[dependencies]
fncc = { path = "../../crates/fncc" }

[build-dependencies]
fncc-core = { path = "../../crates/fncc-core", version = "0.7.0" }
